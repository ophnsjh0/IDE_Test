# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Environment variables — API keys live here, never commit
.env

# Gmail OAuth secrets — never commit these
backend/credentials.json
backend/token.json
credentials.json
token.json
